meta {
  name: Create Expense
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/accounting/expenses
  body: json
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json {
  {
    "title": "Fuel",
    "amount": 50000,
    "category": "transport",
    "note": "Vehicle fuel"
  }
}
